import { formatContactsCount } from "@/lib/utils";

/** Число контактов словами: «1 контакт», «3 контакта», «11 контактов». */
export const pluralizeContacts = formatContactsCount;

/**
 * Сколько аватаров показываем в стеке до сворачивания в «+N».
 * Ограничение не про «не влезает», а про предсказуемую ширину строки-слота:
 * 12 аватаров растянули бы зону и сломали выравнивание карточек.
 */
export const MAX_AVATARS = 3;
